let firstNum = prompt('Enter the first number');
let secondNum = prompt('Enter the second number');
alert(`The result of addition is ${Number(firstNum) + Number(secondNum)}, subtraction ${Number(firstNum) - Number(secondNum)}, multiplication ${Number(firstNum) * Number(secondNum)} and division ${Number(firstNum) / Number(secondNum)}`);
